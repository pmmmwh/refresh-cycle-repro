import * as React from 'react';
import { fragment } from './DashboardPage';

const Milestones = () => {
  return <span />;
  fragment;
};

export default Milestones;

if (module.hot) {
  module.hot.accept();
}
